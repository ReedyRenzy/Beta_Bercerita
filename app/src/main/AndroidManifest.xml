<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="faisyal.bahasaisyarat.betabercerita">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">
        <activity android:name=".Activities.ui.PhoneLoginActivity"></activity>
        <activity android:name=".Activities.ui.GroupChatActivity" />
        <activity android:name=".Activities.ui.SettingsActivity" />
        <activity
            android:name=".Activities.ui.Fullscreen"
            android:configChanges="keyboardHidden|orientation|screenSize" />
        <activity android:name=".Activities.ui.UploadVideoActivity" />
        <activity android:name=".Activities.ui.RealtimeChatActivity" />
        <activity android:name=".Activities.ui.AdminActivity" />
        <activity
            android:name=".Activities.ui.VideoPlayerActivity"
            android:screenOrientation="landscape" />
        <activity android:name=".Activities.ui.VideoDetailActivity" />
        <activity android:name=".Activities.ui.HomeActivity" />
        <activity android:name=".Activities.ui.RegisterActivity" />
        <activity
            android:name="com.theartofdev.edmodo.cropper.CropImageActivity"
            android:theme="@style/Base.Theme.AppCompat" />
        <activity android:name=".Activities.ui.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>